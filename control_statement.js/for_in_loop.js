//  for in loop in Object

const object={
    cpp: "c++",
    js:"javascrift",
    py:"python"
}
for (const key in object) {
    // console.log(`${key} for ${object[key]}`);

}
    


// for in loop in arrray

const name=["mayuri","rutuja","tejaswini","gayatri","rupali"]
for (const key in name) {
        // console.log(key);   .... return default array value
        console.log(name[key]);
        
        
    }


